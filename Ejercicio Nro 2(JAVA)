public class Par<K, V> {
    private K clave;
    private V valor;

    public Par(K clave, V valor) {
        this.clave = clave;
        this.valor = valor;
    }

    public void mostrarPar() {
        System.out.println("Clave: " + clave + ", Valor: " + valor);
    }

    public static void main(String[] args) {
        
        Par<Integer, String> estudiante = new Par<>(101, "Juan Pérez");
        estudiante.mostrarPar();
        
      
        Par<String, Double> producto = new Par<>("PROD-001", 29.99);
        producto.mostrarPar();
    }
}
